interface:
  display_name: "Agent Coordination"
  short_description: "Coordinate Main/Coordinator, Developer, Reviewer, and Tester Codex sessions."
  default_prompt: "Use a local structured event log and status index so Main/Coordinator plans and assigns work, Developer implements verified changes, and Reviewer/Tester watchers asynchronously review, test, report blockers, and mark handled findings."
